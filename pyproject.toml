[project]
name = "ca-housing"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy<2",
    "pandas",
    "scikit-learn",
    "torch",
    "joblib",
    "structlog",
    "prometheus-client",
]
[project.optional-dependencies]
dev = ["pytest"]